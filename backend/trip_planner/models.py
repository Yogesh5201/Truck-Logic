"""
Data models.

The simulation is stateless — no persistence is required for the core
algorithm. This module is intentionally minimal; a caching model could be
added here to persist routing responses across process restarts if desired.
"""

# No models needed for the stateless simulation.
