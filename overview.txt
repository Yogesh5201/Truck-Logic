================================================================================
  TRUCKLOGIC — HOS-COMPLIANT TRUCK ROUTING & ELD LOG GENERATOR
  Complete Project Overview + Deployment Guide
================================================================================

This file explains the whole project in plain language: what it does, how it is
built, how every Hours-of-Service (HOS) rule is implemented, the exact request/
response flow, and step-by-step instructions to run it locally and deploy it
live (backend on Render, frontend on Vercel).

--------------------------------------------------------------------------------
0. WHAT IS THIS WEBSITE FOR?  (plain-English explanation)
--------------------------------------------------------------------------------
THE SHORT VERSION
    It is a trip planner for truck drivers that also fills out their legally
    required logbook automatically.

THE PROBLEM IT SOLVES
    In the United States, a commercial truck driver is NOT allowed to just
    drive from A to B without stopping. Federal law (the FMCSA "Hours of
    Service" rules) strictly limits how long they can drive and work:
        - They can drive at most 11 hours before a long rest.
        - Their workday window is capped at 14 hours.
        - They must take a 30-minute break after 8 hours of driving.
        - They can only work 70 hours total across any 8 days.
    On top of that, trucks must stop for fuel roughly every 1,000 miles, and
    loading/unloading freight takes about an hour at each end.

    Drivers must also keep a "daily log" — an official grid sheet showing, hour
    by hour, whether they were Driving, On Duty, resting in the Sleeper Berth,
    or Off Duty. Historically this was drawn by hand every single day.

    So a simple question like "I'm in Chicago, I need to pick up in Kansas City
    and deliver in Dallas, and I've already worked 10 hours this week — when and
    where do I legally have to stop, and what do my logs look like?" is actually
    a hard scheduling puzzle.

WHAT THE WEBSITE DOES ABOUT IT
    You type in four simple things:
        1. Where you are now
        2. Where you pick up the load
        3. Where you drop it off
        4. How many hours you've already worked this cycle
    ...and the app instantly does the whole puzzle for you. It:
        - Finds the real driving route on actual roads (via GraphHopper).
        - Walks that route hour by hour and inserts every legally required
          break, 10-hour rest, 34-hour restart, and fuel stop at the correct
          moment and the correct place on the map.
        - Draws the official daily log sheets, already filled in, one per day.

WHO WOULD USE IT
    - Truck drivers planning a trip and wanting to stay legal.
    - Dispatchers / fleet managers estimating realistic delivery times
      (real ETA including mandatory rest, not just raw drive time).
    - Anyone learning how the Hours-of-Service rules actually play out on a
      real route.

WHY IT MATTERS
    Breaking these rules leads to fines, failed inspections, and unsafe, tired
    driving. This tool turns a confusing legal calculation into a clear map and
    a set of ready-to-read log sheets in a couple of seconds.

A QUICK EXAMPLE TO TRY
    Open the app and enter a trip where the driver has almost no hours left —
    for example Current "Houston, TX", Pickup "Oklahoma City, OK", Dropoff
    "Denver, CO", and Current cycle used = 68 — then press Generate. Because the
    driver has almost no hours left, the app shows them driving only about 2
    hours, then being forced into a 34-hour restart before continuing — exactly
    what the law requires. That is the kind of answer this website exists to
    give.

NOTE: This is a simulation for planning and evaluation. It is not certified ELD
compliance software and should not be used as the official record of duty.

--------------------------------------------------------------------------------
1. WHAT THIS APP DOES
--------------------------------------------------------------------------------
You enter four things:
    1. Current location   (e.g. "Chicago, IL")
    2. Pickup location    (e.g. "Kansas City, MO")
    3. Dropoff location   (e.g. "Dallas, TX")
    4. Current cycle used (hours already worked in the 70h/8-day cycle)

The three location boxes have Google-Maps-style AUTOCOMPLETE: as you type, a
dropdown of matching places appears (type "dehr" and you'll see "Dehradun";
type "chic" and you'll see "Chicago"). Pick one, or just type a full address.
Suggestions come from GraphHopper, proxied through the backend so the API key
is never exposed to the browser.

The app returns two outputs:
    A. An interactive MAP of the truck route with markers for the start,
       pickup, dropoff, fuel stops, and mandatory rest breaks.
    B. FMCSA DRIVER'S DAILY LOG sheets — the official 24-hour grid drawn on an
       HTML5 canvas, one sheet per day, with the stepped duty-status line, a
       "Total Hours" column, a Remarks table, and a 70h/8-day recap.

Everything obeys the FMCSA Hours-of-Service rules for a property-carrying
driver on the 70-hour / 8-day cycle.

--------------------------------------------------------------------------------
2. TECH STACK
--------------------------------------------------------------------------------
Backend  : Python 3.12, Django 5, Django REST Framework, Shapely, Requests
Frontend : React 18, TypeScript, Vite, Material UI (MUI), Zustand, Leaflet
Mapping  : GraphHopper (routing + geocoding) + CARTO / OpenStreetMap tiles
Deploy   : Backend -> Render (Docker) ; Frontend -> Vercel

--------------------------------------------------------------------------------
3. THE BIG IDEA (why this is not just a routing app)
--------------------------------------------------------------------------------
A normal routing API (GraphHopper) gives you a SPATIAL answer: a line on a
map, a total distance, and an "optimal driving time." It knows nothing about
the law.

Truck drivers are bound by TEMPORAL rules: you can only drive 11 hours, only be
on duty 14 hours, must break for 30 minutes after 8 hours of driving, must stop
after 70 on-duty hours in 8 days, etc.

So the core of this project is a "temporal projection engine":
    - It walks along the spatial route step by step.
    - It advances a set of regulatory clocks as it goes.
    - The moment a clock hits its limit, it PAUSES the truck, INJECTS the
      required rest/break/fuel event into the timeline, and uses linear
      interpolation to figure out the exact lat/lon on the route where that
      pause happens (so it can drop a map marker there).
    - The final result is a flat, chronological list of "duty status events"
      that drives both the map markers and the ELD log drawing.

--------------------------------------------------------------------------------
4. HOURS-OF-SERVICE RULES IMPLEMENTED  (backend/trip_planner/services/hos_engine.py)
--------------------------------------------------------------------------------
Five clocks are tracked, each reset at a different point:

    CLOCK               LIMIT                    RESET BY
    -----------------   ----------------------   -------------------------
    drive_since_rest    11 h driving             10h reset, 34h restart
    window_since_rest   14 h on-duty window      10h reset, 34h restart
    drive_since_break   8 h driving before break 30-min break, 10h, 34h
    cycle_used          70 h on-duty / 8 days     34h restart only
    dist_since_fuel     1,000 miles              fuel stop

Available cycle at the start:
    C_avail = 70 - current_cycle_used

Every DRIVING hour AND every ON-DUTY (not driving) hour is subtracted from the
cycle. When the cycle reaches 70, a 34-hour restart is injected and the cycle
resets to 0 (70 available again).

Fixed events always added:
    - 15 min pre-trip inspection  (On Duty)  at the very start
    - 1 hour loading              (On Duty)  at the pickup
    - 1 hour unloading            (On Duty)  at the dropoff
    - 30 min fueling              (On Duty)  every 1,000 miles

How a single driving step decides how far it can go:
    allow = minimum of (
        11 - drive_since_rest,
        14 - window_since_rest,
        8  - drive_since_break,
        70 - cycle_used,
        distance-to-next-fuel / speed,
        distance-left-on-this-leg / speed
    )
When "allow" reaches zero, the engine services the exhausted clock in priority
order and then keeps driving:
    cycle restart (34h) > daily reset (10h) > 30-min break > fuel stop

WORKED EXAMPLE (the tricky one from the spec):
    current_cycle_used = 68  ->  only 2 hours available.
    The truck drives ~2 hours (minus the 0.25h pre-trip that also counts),
    stops, takes a full 34-hour restart, resets to 70 available, and continues.
    This exact case is covered by a unit test:
        test_cycle_almost_exhausted_forces_34h_restart

--------------------------------------------------------------------------------
5. HOW THE MAP IS PRODUCED
--------------------------------------------------------------------------------
- The trip has two legs: Leg 1 = current -> pickup (empty/deadhead),
  Leg 2 = pickup -> dropoff (loaded).
- Each leg is a separate GraphHopper routing request, so the route follows
  real roads. (The free tier uses the "car" profile; "truck"/HGV routing is
  available on paid GraphHopper plans via GRAPHHOPPER_PROFILE.)
- The two legs are stitched into one polyline ("RouteTrack") that also stores
  the cumulative distance at every point.
- point_at(distance) linearly interpolates the [lon, lat] at any distance —
  this is how a rest stop triggered at, say, 605 miles becomes a real map pin.
- Long routes are simplified with the Douglas-Peucker algorithm (Shapely) so
  the map payload stays small and fast.
- The frontend (react-leaflet) draws the OpenStreetMap tiles, the route line,
  and custom teardrop SVG markers (Start / Pickup / Dropoff / Fuel / Rest).
- map.fitBounds() auto-zooms so the whole route is visible.

--------------------------------------------------------------------------------
6. HOW THE ELD DAILY LOG IS DRAWN  (frontend/src/components/EldCanvas.tsx)
--------------------------------------------------------------------------------
The log matches the official FMCSA "Driver's Daily Log" paper form:
    - Header block: Total Miles Today, Day X of N, Carrier, From, To.
    - A 24-hour grid: X axis = time (Midnight -> Noon -> Midnight), with each
      hour split into four 15-minute ticks.
    - Y axis = four duty rows, labeled exactly like the form:
          1. Off Duty
          2. Sleeper Berth
          3. Driving
          4. On Duty (not driving)
    - The duty status is a continuous STEPPED line: horizontal while a status
      holds, vertical when the status changes.
    - A "Total Hours" column on the right shows each row's total for the day,
      plus a grand total (should equal 24).
    - Below the grid: a Remarks table listing the time, new status, city/state
      location (reverse-geocoded), and a note for every status change.
    - A Recap panel shows the 70h/8-day math: on-duty today, total last 8 days,
      and hours available tomorrow.

MULTI-DAY LOGIC (frontend/src/components/eldLogic.ts):
    A trip longer than 24 hours needs multiple sheets. The flat event list is
    split into 24-hour chunks. An event that crosses midnight (e.g. a 10-hour
    sleeper period from hour 20 to hour 30) is cut at the boundary: sheet 1
    draws 20:00 -> 24:00, sheet 2 draws 00:00 -> 06:00. Each day is a tab.

--------------------------------------------------------------------------------
7. API CONTRACT
--------------------------------------------------------------------------------
Endpoint:  POST  /api/v1/simulate-trip/

Request body (JSON):
    {
      "current_location": "Chicago, IL",
      "pickup_location":  "Kansas City, MO",
      "dropoff_location": "Dallas, TX",
      "current_cycle_used": 10.0
    }

Response body (JSON), three main sections:
    trip_summary     -> totals: distance, duration, driving hours, counts of
                        fuel stops / resets / restarts, number of log sheets,
                        cycle available at start.
    route_geometry   -> a GeoJSON FeatureCollection (LineString) for the map.
    timeline_events  -> the chronological list of duty-status blocks; each has
                        event_type, status, start_hr, end_hr, duration_hrs,
                        start_coord, end_coord, distance_mi, location_label.
    waypoints        -> resolved coordinates + labels for current/pickup/dropoff.

Health check:  GET /api/v1/health/  ->  {"status": "ok"}

Autocomplete:  GET /api/v1/autocomplete/?q=<partial>  ->
    {"suggestions": [{"label": "Chicago, Illinois, United States",
                      "lon": -87.62, "lat": 41.87}, ...]}
    Proxies GraphHopper geocoding (autocomplete=true) so the key stays on the
    server. Returns an empty list on any hiccup — it never blocks typing.

Validation: addresses must be non-blank; current_cycle_used must be 0-70.
Bad input returns HTTP 400 with a field-level error message.

--------------------------------------------------------------------------------
8. PROJECT STRUCTURE
--------------------------------------------------------------------------------
truck routing/
  README.md                 Full markdown docs
  overview.txt              This file
  render.yaml               Render deploy blueprint (backend)
  .gitignore

  backend/
    manage.py
    requirements.txt
    Dockerfile              Container build for Render/Heroku/Fly
    .env.example            Copy to .env and add your GraphHopper key
    config/                 Django project (settings, urls, wsgi)
    trip_planner/
      views.py              Thin DRF endpoint
      serializers.py        Input validation
      constants.py          Every HOS number in one place
      urls.py
      services/
        graphhopper_client.py  GraphHopper geocode + routing + reverse-geocode
                            (cached, typed errors, offline mock fallback)
        geometry.py         RouteTrack + linear interpolation (framework-free)
        hos_engine.py       The HOS finite state machine (framework-free)
        simulator.py        Orchestration + response assembly + simplification
      tests/
        test_hos_engine.py  9 unit tests (limits, restart edge case, fuel, etc.)

  frontend/
    package.json
    vite.config.ts
    vercel.json             SPA rewrite config for Vercel
    .env.example            Copy to .env ; set VITE_API_BASE_URL
    index.html
    src/
      main.tsx              App bootstrap (theme + fonts + leaflet css)
      App.tsx               Layout: form on the left, results on the right
      types.ts              TypeScript mirror of the API response
      api/client.ts         Typed fetch wrapper with error handling
      store/tripStore.ts    Zustand global state (loading/result/error)
      theme/theme.ts        MUI theme + shared status/event colors
      components/
        TripForm.tsx        Inputs with address autocomplete + validation
        TripSummary.tsx     Metric cards
        TripMap.tsx         react-leaflet map, custom markers, fitBounds
        EldCanvas.tsx       The FMCSA 24-hour grid + stepped line (canvas)
        EldLogViewer.tsx    Header, day tabs, remarks table, recap
        eldLogic.ts         Splits the timeline into per-day sheets
        format.ts           Time/label formatting helpers

--------------------------------------------------------------------------------
9. RUN IT LOCALLY  (step by step)
--------------------------------------------------------------------------------
Prerequisites: Python 3.12+ and Node 18+ installed.

--- BACKEND ---
    cd backend
    python -m venv .venv
    # Activate the virtual environment:
    #   Windows:        .venv\Scripts\activate
    #   macOS / Linux:  source .venv/bin/activate
    pip install -r requirements.txt
    copy .env.example .env         (Windows)   /   cp .env.example .env  (mac/linux)
    # Open .env and paste your GraphHopper key on the GRAPHHOPPER_API_KEY= line.
    # (You CAN skip the key for now — the app runs with a built-in mock route.)
    python manage.py runserver 8000

    Backend is now at:  http://localhost:8000
    Test it:            http://localhost:8000/api/v1/health/

--- FRONTEND (in a second terminal) ---
    cd frontend
    npm install
    copy .env.example .env         (Windows)   /   cp .env.example .env  (mac/linux)
    # .env default already points at http://localhost:8000/api/v1
    npm run dev

    Frontend is now at: http://localhost:5173
    Open it in a browser, type the three locations (autocomplete will help),
    set the cycle hours, and press "Generate".

--------------------------------------------------------------------------------
10. ADD YOUR GRAPHHOPPER API KEY
--------------------------------------------------------------------------------
1. Sign up (free) at:  https://www.graphhopper.com/  (Dashboard -> API Keys)
2. Copy your API key.
3. Open  backend/.env  and set:
        GRAPHHOPPER_API_KEY=your-key-here
4. Restart the backend (Ctrl+C, then `python manage.py runserver 8000`).

Now the app uses real road routing, real geocoding, and real city/state names
in the Remarks. Without the key, it still works using a straight-line mock so
you are never blocked.

   Profile note: GraphHopper's free tier supports car/bike/foot. The "truck"
   profile needs a paid plan; set GRAPHHOPPER_PROFILE=truck if you have it.

--------------------------------------------------------------------------------
11. RUN THE TESTS
--------------------------------------------------------------------------------
    cd backend
    python manage.py test

Expected: "Ran 9 tests ... OK". These verify the 11h/14h/8h limits, the 70h
cycle with the 34-hour restart edge case, fuel-stop injection, and that the
timeline has no gaps.

Frontend build check:
    cd frontend
    npm run build

--------------------------------------------------------------------------------
12. DEPLOY THE BACKEND TO RENDER  (Docker)
--------------------------------------------------------------------------------
1. Push this whole repo to GitHub (see section 14).
2. Go to https://render.com  ->  sign in  ->  "New +"  ->  "Blueprint".
3. Connect your GitHub repo. Render reads render.yaml automatically and creates
   a Docker web service named "truck-routing-api".
4. In the service's "Environment" settings, set:
        GRAPHHOPPER_API_KEY   = your GraphHopper key
        CORS_ALLOWED_ORIGINS  = https://your-frontend.vercel.app
   (DJANGO_SECRET_KEY is auto-generated; DJANGO_DEBUG is already "False" and
   DJANGO_ALLOWED_HOSTS is already ".onrender.com" from render.yaml.)
   You don't know the Vercel URL yet — that's fine. Put a placeholder now and
   come back to fix CORS_ALLOWED_ORIGINS after section 13 (step 6).
5. Click "Apply" / "Create". Wait for the build to finish (a few minutes).
6. Your API is live at:  https://truck-routing-api.onrender.com
   Verify:               https://truck-routing-api.onrender.com/api/v1/health/
   (Should return {"status": "ok"}.)

   NOTE: Render's free tier sleeps after inactivity; the first request after a
   nap can take ~30-50 seconds to wake. That is normal.

Alternative (no Blueprint): create a "Web Service", point it at the repo, set
Root Directory to "backend", Runtime = Docker, and add the same env vars.

--------------------------------------------------------------------------------
13. DEPLOY THE FRONTEND TO VERCEL
--------------------------------------------------------------------------------
1. Go to https://vercel.com  ->  "Add New..."  ->  "Project".
2. Import your GitHub repo.
3. IMPORTANT settings:
        Root Directory      = frontend
        Framework Preset    = Vite   (auto-detected)
        Build Command       = npm run build   (default)
        Output Directory    = dist            (default)
4. Add an Environment Variable:
        VITE_API_BASE_URL = https://truck-routing-api.onrender.com/api/v1
   (use YOUR Render backend URL, and keep the /api/v1 suffix)
5. Click "Deploy". After ~1 minute you get a live URL like:
        https://your-frontend.vercel.app
6. Go BACK to Render and make sure CORS_ALLOWED_ORIGINS is set to this exact
   Vercel URL (no trailing slash), then save so the backend accepts the
   browser requests.

--------------------------------------------------------------------------------
14. PUSH TO GITHUB  (the link you give the client)
--------------------------------------------------------------------------------
The repository is already initialized and fully committed locally. Your real
.env files and the .claude/ folder are gitignored, and the API key has been
kept out of the entire git history — so it is safe to publish.

STEP 1 — (optional but recommended) confirm nothing secret is tracked:
    cd "path/to/truck routing"
    git ls-files | grep -E "\.env$"        # should print NOTHING
    git grep -i "your-api-key-value"         # should print NOTHING

STEP 2 — create an EMPTY repo on GitHub:
    - Go to https://github.com/new
    - Repository name: e.g.  truck-routing-eld
    - Choose Public (so the client can open the link) or Private (then add the
      client as a collaborator under Settings -> Collaborators).
    - IMPORTANT: do NOT check "Add a README", ".gitignore", or "license" —
      the repo must be empty so the first push isn't rejected.
    - Click "Create repository".

STEP 3 — connect and push. Copy the HTTPS URL GitHub shows you, then run:
    cd "path/to/truck routing"
    git remote add origin https://github.com/YOUR_USERNAME/truck-routing-eld.git
    git branch -M main          # renames the local 'master' branch to 'main'
    git push -u origin main

    (If GitHub asks you to log in, use your username and a Personal Access
    Token as the password — create one at
    https://github.com/settings/tokens with the "repo" scope. Or install the
    GitHub CLI: `gh auth login`, then push.)

STEP 4 — verify on github.com: refresh the repo page. You should see the
    README render, plus the backend/ and frontend/ folders. Open a couple of
    files to confirm they're there.

STEP 5 — THE LINK TO SEND THE CLIENT:
        https://github.com/YOUR_USERNAME/truck-routing-eld

    If the repo is Private, either invite the client as a collaborator, or make
    it Public for grading (Settings -> General -> Danger Zone -> Change
    visibility). A private repo with no access will look empty to them.

Pushing later commits (after the first push):
    git add -A
    git commit -m "your message"
    git push

--------------------------------------------------------------------------------
15. DEPLOYMENT CHECKLIST (in order)
--------------------------------------------------------------------------------
    [ ] Get a GraphHopper API key
    [ ] Push repo to GitHub (section 14) and confirm the link opens
    [ ] Make sure the client can access it (Public, or add as collaborator)
    [ ] Deploy backend to Render (Blueprint), set GRAPHHOPPER_API_KEY
    [ ] Copy the Render backend URL; verify /api/v1/health/ returns ok
    [ ] Deploy frontend to Vercel, set VITE_API_BASE_URL to the Render URL
    [ ] Copy the Vercel frontend URL
    [ ] Set CORS_ALLOWED_ORIGINS on Render to the Vercel URL, save (redeploys)
    [ ] Open the Vercel URL, run a sample trip, confirm map + logs render
    [ ] Record the 3-5 minute Loom walkthrough
    [ ] Send the client: GitHub link + live Vercel link + Loom link

--------------------------------------------------------------------------------
16. LOOM WALKTHROUGH — QUICK OUTLINE (3-5 min)
--------------------------------------------------------------------------------
    0:00  Intro: what the app does (inputs -> route + ELD logs).
    0:30  Live demo: enter LA -> Denver -> New York (autocomplete helps). Show
          the map (markers, route), then the multi-day ELD logs (tabs, stepped
          line, remarks, recap).
    1:30  Enter a near-full cycle trip (cycle used = 68) to show the 34-hour
          restart appearing in the logs — the standout HOS edge case.
    2:15  Architecture: explain the spatial->temporal idea. Open hos_engine.py
          and show the five clocks and the "allow = min(...)" driving step.
    3:15  Show geometry.py point_at() — how a time-based rest becomes a map pin.
    3:45  Frontend: EldCanvas.tsx grid + stepped line; midnight-split pagination.
    4:30  Wrap up: GraphHopper integration, tests passing, deployed.

--------------------------------------------------------------------------------
17. LOOM WALKTHROUGH — FULL WORD-FOR-WORD SCRIPT
--------------------------------------------------------------------------------
Tips before you record:
  - Have the app open at the Vercel (or localhost:5173) URL, plus your editor.
  - Set the theme to light mode to start (you'll toggle dark mode on camera).
  - Total spoken time is ~4.5 min at a natural pace. The [ACTION] lines tell you
    what to click; the plain text is what to say. Trim any section if you run long.

................................................................................
[0:00-0:25]  INTRO — WHAT & WHY
................................................................................
[ACTION] Show the app's landing screen (empty state with the form on the left).

"Hi — this is TruckLogic, a full-stack app that turns a trucking trip into a
fully Hours-of-Service compliant plan. You give it four things — where you are,
where you pick up, where you drop off, and how many hours you've already worked
in your cycle — and it returns two things: an interactive route map with every
required stop, and FMCSA driver's daily log sheets, drawn automatically.

It's a Django REST backend, a React and TypeScript frontend, and it uses the
GraphHopper API for real road routing and geocoding."

................................................................................
[0:25-1:05]  LIVE DEMO — THE HAPPY PATH
................................................................................
[ACTION] Type into the three location fields — Current "Los Angeles, CA",
         Pickup "Denver, CO", Dropoff "New York, NY" (pick from the autocomplete
         dropdown as it appears), set cycle used to 8, then "Generate route &
         logs". While the skeleton loaders show, keep talking.

"Let me run a real trip. As I type each location, notice the address
autocomplete — I'm starting in Los Angeles, picking up in Denver, and dropping
off in New York. While it loads we show skeleton
placeholders that match the final layout, not just a spinner — so it feels fast
and intentional.

[ACTION] Results appear. Point at the six metric cards along the top.

And here's the result. Up top, a summary: total distance — around 2,800 miles
of real road routing from GraphHopper — total trip time including every rest,
the cycle hours still available, and counts of rest stops, fuel stops, and how
many daily log sheets this trip needs. Here it's five days."

................................................................................
[1:05-1:45]  THE MAP
................................................................................
[ACTION] Move the cursor over the map. Hover a couple of markers to open popups.

"The map auto-fits to the whole route. The line animates in as it draws. Every
stop is a distinct, color-coded marker — green for the start, amber for pickup,
red for dropoff, violet for fuel, indigo for the mandatory rest breaks. The
start and dropoff pins have a subtle animated pulse so your eye finds them
first. Hovering any marker gives an elegant popup with the stop type, the time
into the trip, and the reverse-geocoded city and state — that location text
also comes from GraphHopper."

[ACTION] Scroll down to the Daily Log section. Click through the day tabs.

"Below that are the ELD logs. Because this trip spans five days, the app
generates five separate 24-hour sheets — one per tab."

................................................................................
[1:45-2:25]  THE ELD LOG SHEET (the hard part)
................................................................................
[ACTION] Point at the grid: the hour header, the four rows, the stepped line,
         then the Total Hours column, then the Remarks table and Recap panel.

"This is drawn on an HTML5 canvas to match the real FMCSA paper log exactly.
The horizontal axis is the 24-hour day — Midnight, Noon, Midnight — with every
hour split into 15-minute ticks. The four rows are the duty statuses: Off Duty,
Sleeper Berth, Driving, and On Duty not driving. The thick line steps between
rows every time the status changes — that's the driver's day.

On the right is the Total Hours column, and it always sums to 24. Below the
grid, the Remarks table lists every duty change with its time and location, and
this Recap panel does the 70-hour, 8-day math: hours on duty today, total over
the last 8 days, and hours available tomorrow.

One nice detail: a rest that crosses midnight is split automatically — it ends
one sheet at midnight and continues on the next from hour zero."

................................................................................
[2:25-2:55]  THE STANDOUT EDGE CASE — 34-HOUR RESTART
................................................................................
[ACTION] Enter a short trip (e.g. Houston -> Oklahoma City -> Denver) and set
         Current cycle used to 68, then Generate.

"Now the case that really tests the logic. This driver starts with 68 of their
70 cycle hours already used — so only 2 hours of legal driving left. Watch what
happens.

[ACTION] When results load, open the log and point to the 34-hour block.

The engine lets them drive just under two hours, then it's forced to stop and
take a full 34-hour restart to reset the cycle — you can see that long block
right here in the log — and only then does driving resume. That single behavior
is the difference between a toy and a correct HOS simulation."

................................................................................
[2:55-3:00]  POLISH — DARK MODE
................................................................................
[ACTION] Click the sun/moon toggle in the header.

"Quick note on polish — full light and dark mode. The map tiles, the canvas
log, every component adapts, and the choice is remembered."

................................................................................
[3:00-3:50]  ARCHITECTURE — SPATIAL TO TEMPORAL
................................................................................
[ACTION] Switch to your editor. Open backend/trip_planner/services/hos_engine.py.

"Here's the core idea. GraphHopper gives us a purely spatial answer — a line
and a distance. But the law is about time. So this HOS engine walks along the
route and runs five clocks at once: the 11-hour driving limit, the 14-hour
window, the 8-hour-till-break rule, the 70-hour cycle, and distance since the
last fuel stop.

[ACTION] Scroll to the "allow = min(...)" block in _drive_distance.

The whole thing lives in this one expression. Each driving step advances only
as far as the nearest binding limit — the minimum across all the clocks. When a
limit hits zero, the engine injects the right rest — a 34-hour restart, a
10-hour reset, a 30-minute break, or a fuel stop — and then keeps going."

[ACTION] Open services/graphhopper_client.py; scroll to the typed error classes.

"The GraphHopper client wraps geocoding and routing, caches responses so we
never hit the rate limit twice, and has typed errors for an invalid key, rate
limiting, network failures, and no-route-found — each mapped to a friendly
message in the UI."

................................................................................
[3:50-4:20]  ARCHITECTURE — PROJECTING TIME BACK ONTO SPACE
................................................................................
[ACTION] Open services/geometry.py; point to point_at().

"The other clever bit is here. When a rest triggers at, say, 605 miles in, this
point_at function linearly interpolates the exact latitude and longitude along
the route polyline — that's how a time-based event becomes a real pin on the
map. The backend does all of this and returns one flat list of events that
drives both the map and the logs — the browser never re-computes anything."

[ACTION] Open frontend/src/components/EldCanvas.tsx briefly, then eldLogic.ts.

"On the frontend, this canvas file draws the grid and the stepped line, and
eldLogic.ts is what splits the timeline into per-day sheets and handles the
midnight crossings."

................................................................................
[4:20-4:45]  WRAP UP
................................................................................
[ACTION] Optionally show the terminal with `python manage.py test` passing.

"To wrap up: ORS was fully replaced with GraphHopper with no loss of
functionality, the HOS engine is covered by unit tests including that 34-hour
restart case, the frontend builds clean and code-splits the heavy map and log
components, and it's deployed with the backend on Render and the frontend on
Vercel. Thanks for watching."

................................................................................
RECORDING CHECKLIST
................................................................................
    [ ] App open at your live URL, backend awake (hit it once so Render wakes)
    [ ] Editor open with hos_engine.py, graphhopper_client.py, geometry.py,
        EldCanvas.tsx, eldLogic.ts in tabs
    [ ] Start in light mode; toggle dark mode once on camera
    [ ] Run BOTH trips: LA -> Denver -> NY (multi-day) and a short trip with
        cycle used = 68 (34-hour restart)
    [ ] Show the address autocomplete dropdown at least once while typing
    [ ] Keep it 3-5 minutes; the architecture section is the first to trim
    [ ] Mention GraphHopper by name at least once (integration requirement)

================================================================================
  End of overview. See README.md for the same content in markdown form.
================================================================================
